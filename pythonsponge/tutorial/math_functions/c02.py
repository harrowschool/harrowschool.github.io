import math

nums = input("enter comma separated decimals: ").split(",")
round_choice = input("choose 1) ceil, 2) floor, 3) round 0dp: ")

total = 0

for num in ____:

  val = float(___)
  
  if round_choice == "1":
    total += math.____(val)
  elif round_choice == "2":
    total += math.____(val)
  else:
    total += int(_____(val, __))

print(total)