# Inserting into a correct place

Input a firstname from the user.

Insert it into its correct (alphabetically ordered) position into the existing list.

Output the updated list with the new name in it.
