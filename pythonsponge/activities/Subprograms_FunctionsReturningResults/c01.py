# GLOBAL VARIABLES

textChoice = ""
numChoice = 0

# SUBPROGRAMS

def getNonEmptyString():
  # local variables
  msg = ""

  # ==> rearrange the following lines - use ALT and up/down arrows to move a line
    if msg != "":
    msg = input("enter text: ")
      return msg
  while True:
  # end rearrange section

def getPositiveInteger():
  # local variables
  numEntry = ""

  # ==> rearrange the following lines and complete the gaps
      return int(___)
    if numEntry.isd_____() and int(___) > 0:
    numEntry = input("enter a positive whole number: ")
  while True:
  # end rearrange section

# MAIN PROGRAM

# ==> add two suitable function calls and store the results in appropriate global variables


print("The string {} multiplied by {} gives {}".format(textChoice, numChoice, textChoice * numChoice))
